---
import { fetchLatestToolRelease, stripToolDoctype } from '../../lib/tool-release';

const toolHtml = await fetchLatestToolRelease({
  repo: 'bip39-words-list-binary-index',
  asset: 'bip39-words-list.html',
});
const content = stripToolDoctype(toolHtml);
---
<Fragment set:html={content} />
